version: 2
updates:
  - package-ecosystem: "cargo"
    directory: "/src-tauri"
    schedule:
      interval: "weekly"
    ignore:
      # Blocked by Tauri's current GTK/WebKit dependency chain.
      - dependency-name: "glib"
        versions:
          - ">= 0.20.0"
