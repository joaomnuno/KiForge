# Ensure consistent line endings across platforms.
# Without this, Windows checkouts convert text files to CRLF and
# `prettier --check` fails on CI because the config expects LF.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.gz binary
*.tar binary
