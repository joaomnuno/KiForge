# Projects — Start a new design

- **Location**: [src/features/projects/ProjectsPage.tsx](../../src/features/projects/ProjectsPage.tsx) (hero section)
- **Label**: "Start a new design"
- **Variant**: Primary (`<Link>` styled with `button button--primary`).
- **Trigger**: Always present in the page hero on `/projects`.
- **Workflow**: Navigates to `/projects/new` with no template preselected.
- **Disabled when**: Never.
- **Why it exists**: The hero-level call to action for users who land on
  the project list and want to create a new project from scratch.
