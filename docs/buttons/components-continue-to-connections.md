# Components — Continue to connections

- **Location**: [src/features/components/ComponentsPage.tsx](../../src/features/components/ComponentsPage.tsx) (inspector panel)
- **Label**: "Continue to connections"
- **Variant**: Primary (`<Link>` styled with `button button--primary button--block`).
- **Trigger**: Always visible in the inspector when a project is open.
- **Workflow**: Navigates to `/workspace/connections`.
- **Disabled when**: Never.
- **Why it exists**: The next step in the planning workflow once devices
  have been added.
